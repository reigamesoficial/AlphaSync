# =============================================================================
# AlphaSync — VPS 3 (DATABASE) — Variáveis de ambiente
# =============================================================================

# Credenciais do banco
POSTGRES_USER=alphasync
POSTGRES_PASSWORD=SUBSTITUA_SENHA_FORTE_DO_BANCO
POSTGRES_DB=alphasync

# Porta local (não exposta — apenas para referência)
# NUNCA expor na internet. Acesso só via IP privado da VPS 3.
POSTGRES_PORT=5432
